# TimeToSleep 宠物化改造

## 背景

当前产品的核心隐喻是「和自己签一份早睡契约」——用户设定时间、签约、被锁屏。这套逻辑能用，但情感上是冷的：用户是被管教者，锁屏是惩罚，streak 是 KPI。

我们要把整个产品从「自律工具」变成「领养一只猫」：

- 猫猫每晚来你电脑上过夜，趴在屏幕上睡觉，你就没法用了
- 你不是被锁屏，而是把屏幕让给了猫猫
- 用户的角色从「被约束的人」变成「照顾者」

这个改造分两条线并行：**锁屏界面**和 **Onboarding + CLI 文案**。

---

## Part A：锁屏界面（LockScreen.swift）

### 目的

锁屏是用户每晚盯着看的最后一个画面，也是整个产品的「脸」。现在的锁屏是：大时钟 + streak 成就动画 + 励志文案。改造后应该是：猫猫睡在床上，整个画面安静、温暖、有陪伴感。

### TODO

- [ ] 去掉 StreakAchievementView（glow / orbit / seal 三套动画全部移除）
- [ ] 画一个睡觉场景：床 + 猫猫蜷在被子里 + 呼吸起伏动画
  - CatBedtimePreview.swift 里已经有 `drawSleepingCatInBed` / `drawBed` 等绘制函数，可以复用或简化搬过来
  - 不需要完整的关灯叙事动画，只要最终态：猫猫已经睡着了
  - 考虑加几个飘动的 zzz 气泡（简单的 CALayer 上浮 + 淡出）
- [ ] 把 quotes 换成猫猫口吻（"嘘……猫猫已经睡着了"、"猫猫替你守着，去睡吧"、"你的床比屏幕舒服多了"）
- [ ] 时钟保留，位置可能需要调整（给猫猫腾地方）
- [ ] 底部信息改写：
  - "明早 07:00 解锁" → "猫猫早上 07:00 走"
  - 新增一行动态倒计时："猫猫还有 X 小时 X 分钟醒来"，每分钟更新
- [ ] 整体色调和布局重新调一下，现在的深蓝渐变底色可以保留但要和猫猫场景搭配
- [ ] ESC 提示保持不变（功能性文字不改）

### 参考

- `src/overlay/CatBedtimePreview.swift` — 已有完整的猫猫/床/灯绘制代码
- `src/overlay/LockScreen.swift:271-687` — 要被替换的 StreakAchievementView

---

## Part B：Onboarding 领养流程 + CLI 文案

### 目的

用户第一次接触产品是通过 `zzz init`，这是建立情感联结的唯一机会。现在的 init 是签契约：设时间、选日期、输入「和晚睡说再见」。改造后应该是领养一只猫：猫猫需要一个地方过夜，你愿意收留它吗。

同时所有日常 CLI 命令（`zzz`、`zzz tonight off`、`zzz config`、通知、卸载）的文案都要跟着换成猫猫叙事。

### TODO

#### init.sh 领养流程
- [x] 开场：猫猫 ASCII art + "有一只小猫需要一个地方过夜"
  - 替换现在的 ui_moon + 契约说明
- [x] 时间设定的措辞：
  - "你想几点睡觉？" → "猫猫晚上几点到你这？"
  - "你想几点起床？" → "猫猫早上几点走？"
- [x] 周几来住：
  - "哪几天启用？" → "猫猫每周哪几天来？"
  - 未选中日的描述："其他日子猫猫自己在外面浪"
- [x] Wind-down：
  - "提前多久开始提醒？" → "猫猫提前多久到门口晃悠？"
- [x] 确认：
  - 不再展示「早睡契约」，改为领养确认卡片
  - 输入「我愿意遵守承诺让猫猫好好休息」代替「和晚睡说再见」
- [x] 完成后的文案："猫猫已经记住你家地址了。今晚 XX:XX，它会来的。"

#### bin/zzz CLI 文案
- [x] `zzz`（默认视图）：
  - "今晚 23:00 锁定" → "猫猫今晚 23:00 来"
  - "还有 X 小时 X 分钟" → "猫猫还有 X 小时到"
  - 去掉 streak 显示（后续用宠物成长替代）
  - "今天不在启用日" → "猫猫今天不来，放松吧"
- [x] `zzz tonight off`：
  - "你想跳过今晚的锁定" → "今晚让猫猫别来了？"
  - "请输入跳过原因" → "跟猫猫说一声为什么今晚不来？"
- [x] `zzz config`：
  - "睡觉时间" → "猫猫到达"
  - "起床时间" → "猫猫离开"
  - "启用日" → "猫猫来住的日子"
- [x] `zzz log`：
  - ✓ → 🐱（猫猫来过了）
  - ○ → 💤（猫猫没来）
- [x] `zzz uninstall`：
  - "你的 TimeToSleep 之旅" → "你和猫猫的日子"
  - "我不再需要TimeToSleep" → "送走猫猫"
  - 结尾："猫猫带着行李走了。它会记得你的。"
- [x] `zzz help` / `ui_moon`：把月亮 ASCII 换成猫猫

#### daemon.sh 通知
- [x] Wind-down 通知：
  - "睡前提醒：还有 30 分钟就要锁定了" → "猫猫还有 30 分钟就到了"
  - "还有 X 分钟锁定，保存你的工作" → "猫猫快到门口了，收拾一下吧"
  - "X 分钟后锁定！" → "猫猫马上就到了！"
  - "还有 1 分钟就要锁定了" → "猫猫到门口了，1 分钟后进来"
- [x] 早安通知：
  - "早安！你已经连续早睡 N 天了" → "猫猫走啦，明晚再来。早安！"

---

## 不做的事（这一轮）

- 不做起床解锁动画（后续再加）
- 不做 streak / 连续天数展示（后续通过宠物成长体系替代）
- 不做渐进式目标（独立 feature）
- 不做 `zzz tonight 23:30` 临时推迟（独立 feature）
- 不动 daemon 的锁屏执行逻辑、bootcheck、launchd 调度（只改文案）
